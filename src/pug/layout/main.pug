block variables

doctype
html(lang='en')
    head
        meta(charset='utf-8')
        title #{title}
    body(class=pageClass)
        .wrapper
            #app
                .content-wrapper
                    block content
